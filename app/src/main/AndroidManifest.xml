<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network & Location Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Camera & Storage Permissions for Face Recognition Penalty Module -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.TransbuddyApp"
        tools:targetApi="31">

        <!-- Google Maps API Key Meta-Data -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDe5pFoiZuP1zGA2yR8amD4iRl5nVlzfqg" />

        <!-- Splash Activity (Marwadi University Branded Launcher Entry Point) -->
        <activity
            android:name=".controllers.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".controllers.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Fleet Dashboard -->
        <activity
            android:name=".controllers.MainActivity"
            android:exported="false" />

        <!-- Fuel & KM Logs -->
        <activity
            android:name=".controllers.FuelLogsActivity"
            android:exported="false"
            android:parentActivityName=".controllers.MainActivity" />

        <!-- Penalties & Fines -->
        <activity
            android:name=".controllers.PenaltiesActivity"
            android:exported="false"
            android:parentActivityName=".controllers.MainActivity" />

        <!-- Driver Operations / Driver Hub -->
        <activity
            android:name=".controllers.OperationsActivity"
            android:exported="false"
            android:parentActivityName=".controllers.MainActivity" />

        <!-- Violations & Fee Alerts -->
        <activity
            android:name=".controllers.ViolationsActivity"
            android:exported="false"
            android:parentActivityName=".controllers.MainActivity" />

        <!-- Home Search Hub (GR & Pickup Point Search) -->
        <activity
            android:name=".controllers.HomeSearchActivity"
            android:exported="false"
            android:parentActivityName=".controllers.MainActivity" />

        <!-- Face Recognition Penalty Activity -->
        <activity
            android:name=".controllers.FaceRecognitionPenaltyActivity"
            android:exported="false"
            android:parentActivityName=".controllers.HomeSearchActivity" />

        <!-- Student Profile & Penalty Activity -->
        <activity
            android:name=".controllers.StudentDetailActivity"
            android:exported="false"
            android:parentActivityName=".controllers.HomeSearchActivity" />

        <!-- Emergency Notifications -->
        <activity
            android:name=".controllers.EmergencyNotificationsActivity"
            android:exported="false"
            android:windowSoftInputMode="stateHidden"
            android:parentActivityName=".controllers.MainActivity" />

        <!-- FileProvider for Full-Resolution Camera Photo Captures -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>